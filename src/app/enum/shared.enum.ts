export enum TypeDocument {
    USER = 'user',
    ENTRY_EXIT = 'entry-exit',
    ITEMS = 'items'
}

export enum TypeEntry {
    ENTRY = 'entry',
    EXIT = 'exit'
}

export enum TypeStore{
    UI = 'ui',
    USER = 'user',
}